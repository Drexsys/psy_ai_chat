version: "3.8"
services:
  database:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=psy_ai_chat
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - 5433:5432

volumes:
  postgres_data:
